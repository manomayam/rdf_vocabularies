// THIS FILE IS GENERATED. ONE SHOULD NOT MODIFY IT
//! This module provides terms for `xhv` vocabulary
//!
//! ## Vocabulary:
//!
//! |||
//! |-----|-----|
//! |**Title**||
//! |**Prefix**|xhv|
//! |**Namespace base IRI**|[http://www.w3.org/1999/xhtml/vocab#](http://www.w3.org/1999/xhtml/vocab#)|
//! |**Description**||
//! |**Is defined by**|[https://www.w3.org/2012/pyRdfa/extract?uri=https%3A%2F%2Fwww.w3.org%2F1999%2Fxhtml%2Fvocab.xhtml&format=turtle&rdfagraph=output&vocab_expansion=false&rdfa_lite=false&embedded_rdf=true&space_preserve=true&vocab_cache=true&vocab_cache_report=false&vocab_cache_refresh=false](https://www.w3.org/2012/pyRdfa/extract?uri=https%3A%2F%2Fwww.w3.org%2F1999%2Fxhtml%2Fvocab.xhtml&format=turtle&rdfagraph=output&vocab_expansion=false&rdfa_lite=false&embedded_rdf=true&space_preserve=true&vocab_cache=true&vocab_cache_report=false&vocab_cache_refresh=false)|
//!

use crate::namespace;

namespace!(
    "http://www.w3.org/1999/xhtml/vocab#",;
    /// ``: A message<br>      with important, and usually time-sensitive, information. Also see<br>      alertdialog and status.
    alert, "alert",
    /// ``: A<br>      type of dialog that contains an alert message, where initial focus goes<br>      to the dialog or an element within it. Also see alert and dialog.
    alertdialog, "alertdialog",
    /// ``: alternate designates alternate<br>      versions for a resource.
    alternate, "alternate",
    /// ``: appendix refers to a resource serving<br>      as an appendix in a collection. 
    appendix, "appendix",
    /// ``: A<br>      region declared as a web application, as opposed to a web document.
    application, "application",
    /// ``: A<br>      section of a page consisting of an independent part of a document, page,<br>      or site.
    article, "article",
    /// ``: banner contains the prime heading or<br>      internal title of a page. 
    banner, "banner",
    /// ``: bookmark refers to a bookmark - a link<br>      to a key entry point within an extended document. 
    bookmark, "bookmark",
    /// ``: An input<br>      that allows for user-triggered actions when clicked or pressed.
    button, "button",
    /// ``: chapter refers to a resource serving<br>      as a chapter in a collction. 
    chapter, "chapter",
    /// ``: An<br>      checkable input that has three possible values: true, false, or<br>    mixed.
    checkbox, "checkbox",
    /// ``: cite refers to a resource that defines<br>      a citation. 
    cite, "cite",
    /// ``: A<br>      cell containing header information for a column.
    columnheader, "columnheader",
    /// ``: A<br>      presentation of a select; usually similar to a textbox where users can<br>      type ahead to select an option, or type to enter arbitrary text as a new<br>      item in the list.
    combobox, "combobox",
    /// ``: secondary indicates that the section<br>      supports but is separable from the main content of resource.
    complementary, "complementary",
    /// ``: contentinfo has meta information about<br>      the content on the page or the page as a whole.
    contentinfo, "contentinfo",
    /// ``: contents refers to a resource serving<br>      as a table of contents. 
    contents, "contents",
    /// ``: copyright refers to a copyright<br>      statement for the resource. 
    copyright, "copyright",
    /// ``: definition indicates the definition of<br>      a term or concept.
    definition, "definition",
    /// ``: A dialog<br>      is an application window that is designed to interrupt the current<br>      processing of an application in order to prompt the user to enter<br>      information or require a response. Also see alertdialog.
    dialog, "dialog",
    /// ``: A list<br>      of references to members of a group, such as a static table of<br>    contents.
    directory, "directory",
    /// ``: A<br>      region containing related information that is declared as document<br>      content, as opposed to a web application.
    document, "document",
    /// ``: first refers the first item in a<br>      collection (see also start and top).
    first, "first",
    /// ``: glossary refers to a resource<br>      providing a glossary of terms. 
    glossary, "glossary",
    /// ``: A grid<br>      contains cells of tabular data arranged in rows and columns, like a<br>    table.
    grid, "grid",
    /// ``: A cell<br>      in a grid or treegrid.
    gridcell, "gridcell",
    /// ``: A set of<br>      user interface objects which would not be included in a page summary or<br>      table of contents by an assistive technology.
    group, "group",
    /// ``: A<br>      heading for a section of the page.
    heading, "heading",
    /// ``: help refers to a resource offering<br>      help. 
    help, "help",
    /// ``: icon refers to a resource that<br>      represents an icon. 
    icon, "icon",
    /// ``: A container<br>      for a collection of elements that form an image.
    img, "img",
    /// ``: index refers to a resource providing<br>      an index. 
    index, "index",
    /// ``: itsRules indicates that the designated<br>      resource is an [ITS] rule set.
    itsRules, "itsRules",
    /// ``: last refers to the last resource in a<br>      collection of resources. 
    last, "last",
    /// ``: license refers to a resource that<br>      defines the associated license. 
    license, "license",
    /// ``: An<br>      interactive reference to an internal or external resource.
    link, "link",
    /// ``: A group of<br>      non-interactive list items.
    list, "list",
    /// ``: A widget<br>      that allows the user to select one or more items from a list of<br>    choices.
    listbox, "listbox",
    /// ``: A<br>      single item in a list, listbox, or directory.
    listitem, "listitem",
    /// ``: A type of<br>      live region where new information is added in meaningful order and old<br>      information may disappear. Also see marquee.
    log, "log",
    /// ``: main acts as the main content of the<br>      document. 
    main, "main",
    /// ``: A type<br>      of live region where non-essential information changes frequently. Also<br>      see log.
    marquee, "marquee",
    /// ``: An element<br>      that represents a mathematical expression.
    math, "math",
    /// ``: A type of<br>      widget that offers a list of choices to the user.
    menu, "menu",
    /// ``: A<br>      presentation of menu that usually remains visible and is usually<br>      presented horizontally.
    menubar, "menubar",
    /// ``: An<br>      option in a group of choices contained by a menu or menubar.
    menuitem, "menuitem",
    /// ``: A checkable menuitem that has three possible<br>      values: true, false, or mixed.
    menuitemcheckbox, "menuitemcheckbox",
    /// ``: A<br>      checkable menuitem in a group of menuitemradio roles, only one of which<br>      can be checked at a time.
    menuitemradio, "menuitemradio",
    /// ``: meta refers to a resource that<br>      provides metadata. 
    meta, "meta",
    /// ``: navigation indicates a collection of<br>      items suitable for navigating the document or related documents.
    navigation, "navigation",
    /// ``: next refers to the next resource<br>      (after the current one) in an ordered collection of resources. 
    next, "next",
    /// ``: note indicates the content is<br>      parenthetic or ancillary to the main content of the resource. 
    note, "note",
    /// ``: A<br>      selectable item in a select list.
    option, "option",
    /// ``: p3pv1 refers to a P3P Policy Reference<br>      File [P3P]. 
    p3pv1, "p3pv1",
    /// ``: An<br>      element whose role is presentational and does not need to be mapped to<br>      the accessibility API.
    presentation, "presentation",
    /// ``: prev refers to a previous resource<br>      (before the current one) in an ordered collection of resources. 
    prev, "prev",
    /// ``: An<br>      element that displays the progress status for tasks that take a long<br>    time.
    progressbar, "progressbar",
    /// ``: A<br>      checkable input in a group of radio roles, only one of which can be<br>      checked at a time.
    radio, "radio",
    /// ``: A<br>      group of radio buttons.
    radiogroup, "radiogroup",
    /// ``: A large<br>      perceivable section of a web page or document, that the author feels<br>      should be included in a summary of page features.
    region, "region",
    /// ``: 
    relrev_properties, "relrev-properties",
    /// ``: role indicates the purpose of the<br>      resource. See the XHTML Role<br>      Vocabulary for roles in this vocabulary space, and XHTMLROLE for information on extending the<br>      collection of roles. 
    role, "role",
    /// ``: 
    role_properties, "role-properties",
    /// ``: A row of<br>      cells in a grid.
    row, "row",
    /// ``: A cell<br>      containing header information for a row in a grid.
    rowheader, "rowheader",
    /// ``: search indicates that the section<br>      provides a search facility. 
    search, "search",
    /// ``: section refers to a resource serving<br>      as a section in a collection. 
    section, "section",
    /// ``: A<br>      divider that separates and distinguishes sections of content or groups of<br>      menuitems.
    separator, "separator",
    /// ``: A user<br>      input where the user selects a value from within a given range.
    slider, "slider",
    /// ``: A<br>      form of range that expects a user to select from amongst discrete<br>    choices.
    spinbutton, "spinbutton",
    /// ``: start refers to the first resource in<br>      a collection of resources. 
    start, "start",
    /// ``: A<br>      container whose content is advisory information for the user but is not<br>      important enough to justify an alert. Also see alert.
    status, "status",
    /// ``: stylesheet refers to a resource<br>      serving as a stylesheet for a resource. 
    stylesheet, "stylesheet",
    /// ``: subsection refers to a resource<br>      serving as a subsection in a collection. 
    subsection, "subsection",
    /// ``: A header for<br>      a tabpanel.
    tab, "tab",
    /// ``: A list<br>      of tab elements, which are references to tabpanel elements.
    tablist, "tablist",
    /// ``: A<br>      container for the resources associated with a tab.
    tabpanel, "tabpanel",
    /// ``: Input<br>      that allows free-form text as their value.
    textbox, "textbox",
    /// ``: A<br>      numerical counter which indicates an amount of elapsed time from a start<br>      point, or the time remaining until an end point.
    timer, "timer",
    /// ``: A<br>      collection of commonly used function buttons represented in compact<br>      visual form.
    toolbar, "toolbar",
    /// ``: A<br>      contextual popup that displays a description for an element in a mouse<br>      hover or keyboard focused state. Supplement to the normal tooltip<br>      processing of the user agent.
    tooltip, "tooltip",
    /// ``: top is a synonym for start. 
    top, "top",
    /// ``: A type of<br>      list that may contain sub-level nested groups that can be collapsed and<br>      expanded.
    tree, "tree",
    /// ``: A grid<br>      whose rows can be expanded and collapsed in the same manner as for a<br>    tree.
    treegrid, "treegrid",
    /// ``: An<br>      option item of a tree. This is an element within a tree that may be<br>      expanded or collapsed if it contains a sub-level group of treeitems.
    treeitem, "treeitem",
    /// ``: up refers to a resource "above" in a<br>      hierarchically structured set. 
    up, "up"
);
